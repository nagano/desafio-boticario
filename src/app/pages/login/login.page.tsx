import React from 'react';

// Style
import './login.page.scss';

const LoginPage = (): JSX.Element => {
    return (
        <div className="login default-page-container">
            LOGIN
        </div>
    );
};

export default LoginPage;
